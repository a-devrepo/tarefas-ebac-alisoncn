spring:
  application:
    name: produto-service
  config:
    import: configserver:http://localhost:8888
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
springdoc:
  api-docs:
    path: /produtoservice/api-docs
  swagger-ui:
    path: /produtoservice/swagger-ui.html